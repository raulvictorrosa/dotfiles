zsh-users/zsh-autosuggestions
zsh-users/zsh-completions
zsh-users/zsh-syntax-highlighting

# Individual oh-my-zsh plugins, loaded standalone via antidote's path:
# annotation — no oh-my-zsh framework/core needed, just these plugin files.
# Each one is self-contained (only depends on zsh builtins), confirmed by
# reading the plugin source before switching off of oh-my-zsh entirely.
ohmyzsh/ohmyzsh path:plugins/git
ohmyzsh/ohmyzsh path:plugins/vi-mode
ohmyzsh/ohmyzsh path:plugins/eza

# tmux plugin: aliases (ta, tad, to, ts, tkss, tksv, tl, tmuxconf, tds) plus
# it wraps the `tmux` command itself. ZSH_TMUX_AUTOSTART stays false (default)
# so it never launches tmux on its own; keep an eye on this once fully on
# rmux, since at that point the wrapper's tmux behavior may no longer be wanted.
ohmyzsh/ohmyzsh path:plugins/tmux
